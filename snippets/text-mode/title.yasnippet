#name : Title Block
#key : tit
# --
`(insert comment-start)` ${1:$(make-string (+ 6 (string-width text)) ?\=)}
`(insert comment-start)` ===${1: $(upcase text)}===
`(insert comment-start)` ${1:$(make-string (+ 6 (string-width text)) ?\=)}
$0